---
phase: 01-content-ui-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/components/Hero.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02

must_haves:
  truths:
    - "Hero CTA button has a flat solid magenta background — no gradient, no inset glow, no perspective depth"
    - "The pulsing outer glow span (absolute -inset-1 bg-magenta/30 blur-lg) is removed"
    - "Arrow icon has no circle background wrapper — bare SVG arrow only"
    - "On hover, a white/semi-transparent border glow appears and a magenta box-shadow activates"
    - "whileHover scale(1.03) and whileTap scale(0.97) are preserved"
  artifacts:
    - path: "src/app/components/Hero.tsx"
      provides: "Restyled CTA button — flat magenta, hover border glow, no 3D glass effect"
      contains: "bg-magenta"
  key_links:
    - from: "Hero.tsx motion.a"
      to: "whileHover/whileTap Framer Motion"
      via: "existing whileHover={{ scale: 1.03 }}"
      pattern: "whileHover.*scale"
---

<objective>
Restyle the Hero CTA button: remove the 3D glass/glow effect, replace with flat magenta background and hover border glow.

Purpose: Client reported the perpetual 3D effect feels "weird" — wants a button that reacts on hover instead of always looking 3D.
Output: Hero.tsx with a clean, flat CTA button that activates visually only on hover.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-ui-polish/01-CONTEXT.md
@src/app/components/Hero.tsx
</context>

<interfaces>
<!-- Current CTA button structure (Hero.tsx lines 70-106) — executor replaces this entirely -->
<!-- motion.a wrapper (lines 70-74): keep href="#konzultacio", whileHover scale 1.03, whileTap 0.97 -->
<!-- Remove: outer glow span (lines 77) — absolute -inset-1 rounded-full bg-magenta/30 blur-lg -->
<!-- Remove: gradient + inset box-shadows on inner span (lines 82-85) -->
<!-- Remove: arrow circle wrapper (lines 88-90) — keep only the bare SVG path -->
<!-- Add: flat magenta bg, border-white/20 default, hover border-white/40 + magenta box-shadow -->

Current inner span classes (line 81):
  relative inline-flex items-center gap-3 px-8 py-4 rounded-full font-medium text-white text-base border border-white/10

Current inner span style (lines 83-85):
  background: "linear-gradient(135deg, #ef34ff, #d020e0)",
  boxShadow: "inset 0 1px 0 rgba(255,255,255,0.25), inset 0 -2px 4px rgba(0,0,0,0.3), 0 4px 24px rgba(239,52,255,0.45)"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Hero CTA button — flat magenta with hover border glow</name>
  <files>src/app/components/Hero.tsx</files>
  <read_first>
    - src/app/components/Hero.tsx (full file — understand existing motion.a structure lines 70-106)
  </read_first>
  <action>
Replace the CTA button block (lines 70–106) with a flat-style button. Keep the motion.a wrapper props intact.

NEW button structure:

```tsx
<motion.a
  href="#konzultacio"
  className="group relative inline-flex items-center gap-3 cursor-pointer"
  whileHover={{ scale: 1.03 }}
  whileTap={{ scale: 0.97 }}
>
  <span
    className="relative inline-flex items-center gap-3 px-8 py-4 rounded-full font-medium text-white text-base border border-white/20 transition-all duration-300"
    style={{
      backgroundColor: "#ef34ff",
      boxShadow: "none",
    }}
    onMouseEnter={(e) => {
      (e.currentTarget as HTMLSpanElement).style.borderColor = "rgba(255,255,255,0.4)";
      (e.currentTarget as HTMLSpanElement).style.boxShadow = "0 0 20px rgba(239,52,255,0.5)";
    }}
    onMouseLeave={(e) => {
      (e.currentTarget as HTMLSpanElement).style.borderColor = "rgba(255,255,255,0.2)";
      (e.currentTarget as HTMLSpanElement).style.boxShadow = "none";
    }}
  >
    Díjmentes konzultáció
    <svg
      width="16"
      height="16"
      viewBox="0 0 16 16"
      fill="none"
      className="transform group-hover:translate-x-0.5 transition-transform duration-300"
    >
      <path
        d="M3 8h10m0 0L9 4m4 4L9 12"
        stroke="currentColor"
        strokeWidth="1.5"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  </span>
</motion.a>
```

Key changes from current:
1. Remove the outer glow `<span className="absolute -inset-1 rounded-full bg-magenta/30 blur-lg ...">` entirely
2. Replace `background: "linear-gradient(135deg, #ef34ff, #d020e0)"` with `backgroundColor: "#ef34ff"` (flat solid)
3. Remove all inset box-shadows (the 3D depth effect)
4. Remove the arrow circle wrapper (`<span className="flex items-center justify-center w-8 h-8 rounded-full bg-white/15 ...">`) — keep only the bare SVG
5. Add inline onMouseEnter/onMouseLeave for border and shadow hover transition (Tailwind group-hover doesn't handle box-shadow well)

The secondary text link `<motion.a href="#modszertan"...>` (lines 109-124) stays completely unchanged.
  </action>
  <verify>
    <automated>grep -n "blur-lg\|inset 0 1px\|linear-gradient.*ef34ff\|rounded-full bg-white/15" /Users/merlon/projects/slixol-website/src/app/components/Hero.tsx && echo "FAIL: old 3D glass styles still present" || echo "PASS: old glass styles removed"</automated>
  </verify>
  <done>
- No absolute glow span in Hero.tsx
- No gradient background on CTA button
- No inset box-shadows (3D depth effect gone)
- No arrow circle wrapper
- whileHover scale(1.03) still present
- backgroundColor is solid #ef34ff
- Border glow on hover via onMouseEnter/onMouseLeave
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Flat magenta Hero CTA button with hover border glow. Run `npm run dev` and open http://localhost:3000 to see the hero section.</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in browser
    2. Look at the "Díjmentes konzultáció" CTA button
    3. Confirm: button has solid magenta color, no pulsing outer glow, no 3D depth shadow
    4. Hover over the button — confirm: border brightens and a subtle magenta glow appears around it
    5. Confirm: arrow icon is bare (no circular background wrapper)
    6. Confirm: scale animation still works on hover and click
  </how-to-verify>
  <resume-signal>Type "approved" if it looks correct, or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run lint` passes
2. `grep -n "blur-lg\|bg-magenta/30\|inset 0 1px" src/app/components/Hero.tsx` returns zero results
3. `grep -n "whileHover.*scale.*1.03" src/app/components/Hero.tsx` returns a result (animation preserved)
4. Visual checkpoint: flat button with hover glow confirmed by user
</verification>

<success_criteria>
- Hero CTA has solid #ef34ff magenta background (no gradient)
- No pulsing outer glow span
- No inset box-shadow depth effects
- No circular arrow wrapper
- On hover: border glows white/40, magenta box-shadow 0 0 20px rgba(239,52,255,0.5)
- whileHover scale(1.03) and whileTap scale(0.97) preserved
- `npm run lint` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-ui-polish/01-02-SUMMARY.md`
</output>
