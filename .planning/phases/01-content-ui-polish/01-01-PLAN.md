---
phase: 01-content-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/koszonjuk/page.tsx
  - src/app/components/PartnerLogos.tsx
  - src/app/components/Footer.tsx
  - src/app/components/SlixolModel.tsx
  - src/app/components/Challenge.tsx
  - src/app/components/CaseStudies.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-02

must_haves:
  truths:
    - "No user-visible text reads 'Slixol' (capital S) — every instance is lowercase 'slixol'"
    - "No em-dash (—) appears in rendered user-facing copy — all replaced with en-dash (–)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Metadata with lowercase slixol in titles, OG, Twitter, keywords"
      contains: "slixol –"
    - path: "src/app/components/Footer.tsx"
      provides: "Copyright line with lowercase slixol"
      contains: "slixol. Minden"
    - path: "src/app/components/PartnerLogos.tsx"
      provides: "Body text with lowercase slixol"
      contains: "slixol megoldásaiban"
    - path: "src/app/components/SlixolModel.tsx"
      provides: "Section label and body with correct casing and en-dash"
      contains: "slixol Modell"
    - path: "src/app/components/Challenge.tsx"
      provides: "Body copy with en-dash"
      contains: "szükséges –"
    - path: "src/app/components/CaseStudies.tsx"
      provides: "Card subtitle separator with en-dash"
      contains: "– {c.subtitle}"
  key_links:
    - from: "layout.tsx metadata.title"
      to: "browser tab + OG preview"
      via: "Next.js Metadata API"
      pattern: "slixol –"
---

<objective>
Fix all brand casing and punctuation issues across user-facing content.

Purpose: "Slixol" must always appear lowercase in user-visible text per brand guidelines. Em-dashes (—) in copy must be en-dashes (–).
Output: 7 files with corrected casing and dash characters — no logic changes, only text/string edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-content-ui-polish/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix brand casing — layout.tsx and koszonjuk/page.tsx</name>
  <files>src/app/layout.tsx, src/app/koszonjuk/page.tsx</files>
  <read_first>
    - src/app/layout.tsx (full file — metadata object)
    - src/app/koszonjuk/page.tsx (full file — title metadata)
  </read_first>
  <action>
In src/app/layout.tsx, change every "Slixol" string to "slixol" in user-facing metadata values only. Do NOT change URLs (slixol.com stays as-is), component names, or import paths.

Specific changes (line numbers from current file):
- Line 12: `title: "Slixol –` → `title: "slixol –`
- Line 24 (keywords array): `"Slixol"` → `"slixol"`
- Line 34: `title: "Slixol –` → `title: "slixol –`
- Line 40: `siteName: "Slixol"` → `siteName: "slixol"`
- Line 44: `title: "Slixol –` → `title: "slixol –`

In src/app/koszonjuk/page.tsx:
- Line 6: `title: "Köszönjük – Slixol"` → `title: "Köszönjük – slixol"`

Note: The dashes in titles are already en-dashes (–), not em-dashes — do NOT change them.
  </action>
  <verify>
    <automated>grep -n "Slixol" /Users/merlon/projects/slixol-website/src/app/layout.tsx /Users/merlon/projects/slixol-website/src/app/koszonjuk/page.tsx | grep -v "slixol\.com\|\/\/\|alt=" && echo "FAIL: uppercase Slixol found" || echo "PASS: no uppercase Slixol in metadata"</automated>
  </verify>
  <done>grep finds zero "Slixol" (capital S) matches in layout.tsx and koszonjuk/page.tsx outside of URL strings and code identifiers.</done>
</task>

<task type="auto">
  <name>Task 2: Fix brand casing — components with user-visible "Slixol" text</name>
  <files>src/app/components/PartnerLogos.tsx, src/app/components/Footer.tsx, src/app/components/SlixolModel.tsx</files>
  <read_first>
    - src/app/components/PartnerLogos.tsx (line 63)
    - src/app/components/Footer.tsx (line 190)
    - src/app/components/SlixolModel.tsx (line 143)
  </read_first>
  <action>
Make these targeted text replacements — no other changes to these files:

PartnerLogos.tsx line 63:
  Before: `Több mint 60 vállalkozás bízik a Slixol megoldásaiban.`
  After:  `Több mint 60 vállalkozás bízik a slixol megoldásaiban.`

Footer.tsx line 190:
  Before: `&copy; {new Date().getFullYear()} Slixol. Minden jog fenntartva.`
  After:  `&copy; {new Date().getFullYear()} slixol. Minden jog fenntartva.`

SlixolModel.tsx line 143:
  Before: `<SectionLabel>Slixol Modell</SectionLabel>`
  After:  `<SectionLabel>slixol Modell</SectionLabel>`

NOTE: SlixolModel.tsx line 311 is `alt="Slixol X"` — this is an image alt tag description, leave it unchanged per CONT-01 scope (logo image alt tags stay as-is).
  </action>
  <verify>
    <automated>grep -n "Slixol" /Users/merlon/projects/slixol-website/src/app/components/PartnerLogos.tsx /Users/merlon/projects/slixol-website/src/app/components/Footer.tsx /Users/merlon/projects/slixol-website/src/app/components/SlixolModel.tsx | grep -v "alt=\|slixol-x-magenta\|slixol\.com\|\/\/" && echo "FAIL: uppercase Slixol in user-facing text" || echo "PASS: no uppercase Slixol in user-facing text"</automated>
  </verify>
  <done>All three files have lowercase "slixol" in their user-visible text nodes. Alt tags for logo images are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Replace em-dashes with en-dashes in user-facing copy</name>
  <files>src/app/components/Challenge.tsx, src/app/components/SlixolModel.tsx, src/app/components/CaseStudies.tsx</files>
  <read_first>
    - src/app/components/Challenge.tsx (line 71)
    - src/app/components/SlixolModel.tsx (line 155)
    - src/app/components/CaseStudies.tsx (line 110)
  </read_first>
  <action>
Replace the em-dash character (—, U+2014) with an en-dash (–, U+2013) in JSX text content only. Do NOT touch comment lines.

Challenge.tsx line 71 (JSX text node inside AnimatedText):
  Before: `szükséges — és minél később kezdjük, annál nagyobb a felzárkózás ára.`
  After:  `szükséges – és minél később kezdjük, annál nagyobb a felzárkózás ára.`

SlixolModel.tsx line 155 (JSX text node inside AnimatedText):
  Before: `Nem egy klasszikus ügynökség vagyunk — integrált digitalizációs és növekedési partner,`
  After:  `Nem egy klasszikus ügynökség vagyunk – integrált digitalizációs és növekedési partner,`

CaseStudies.tsx line 110 (JSX text node, subtitle separator):
  Before: `— {c.subtitle}`
  After:  `– {c.subtitle}`

Comment lines containing em-dashes (e.g. `{/* Background glow — blue only */}`) are code comments — leave them unchanged.
  </action>
  <verify>
    <automated>grep -n " — " /Users/merlon/projects/slixol-website/src/app/components/Challenge.tsx /Users/merlon/projects/slixol-website/src/app/components/SlixolModel.tsx /Users/merlon/projects/slixol-website/src/app/components/CaseStudies.tsx | grep -v "\/\*\|\/\/" && echo "FAIL: em-dash in user-facing text" || echo "PASS: no em-dashes in user-facing copy"</automated>
  </verify>
  <done>CaseStudies.tsx, Challenge.tsx, and SlixolModel.tsx have en-dashes (–) in all user-visible text nodes. Comment-only em-dashes are untouched.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run lint` passes with no new errors
2. `grep -rn "Slixol" src/app/ | grep -v "alt=\|slixol-x-magenta\|slixol\.com\|SlixolModel\|\/\/"` returns zero results
3. `grep -rn " — " src/app/components/ | grep -v "\/\*\|\/\/"` returns zero results
</verification>

<success_criteria>
- Zero uppercase "Slixol" in user-facing text nodes (headings, body copy, metadata titles, OG tags, keywords)
- Logo alt tags unchanged: `alt="Slixol"` and `alt="Slixol X"` remain
- Zero em-dashes (—) in JSX text content across all components
- `npm run lint` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-ui-polish/01-01-SUMMARY.md`
</output>
